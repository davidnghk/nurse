<%= simple_form_for(@work) do |f| %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :chi_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
